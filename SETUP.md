# מנהל הוצאות - הוראות הגדרה

## דרישות מקדימות

- Python 3.10+
- חשבון Google

## שלב 1: התקנת ספריות

```bash
pip install -r requirements.txt
```

## שלב 2: הגדרת Google Cloud

1. היכנס ל-[Google Cloud Console](https://console.cloud.google.com/)
2. צור פרויקט חדש (או בחר קיים)
3. הפעל את ה-APIs הבאים:
   - **Google Sheets API**
   - **Google Drive API**

## שלב 3: יצירת Service Account

1. ב-Google Cloud Console, לך ל-**IAM & Admin > Service Accounts**
2. לחץ **Create Service Account**
3. תן שם (למשל: `expense-manager`)
4. לחץ **Create and Continue** (דלג על ההרשאות האופציונליות)
5. לחץ **Done**
6. לחץ על ה-Service Account שנוצר
7. לך ללשונית **Keys**
8. לחץ **Add Key > Create New Key > JSON**
9. הקובץ יורד אוטומטית - שנה את שמו ל-`credentials.json`
10. העבר את הקובץ לתיקיית הפרויקט

## שלב 4: שיתוף הגיליון

אם אתה משתמש בגיליון קיים:
1. פתח את הגיליון ב-Google Sheets
2. לחץ **Share**
3. הוסף את כתובת ה-email של ה-Service Account (נמצאת בקובץ `credentials.json` בשדה `client_email`)
4. תן הרשאת **Editor**

אם אתה יוצר גיליון חדש - האפליקציה תיצור אותו אוטומטית.

## שלב 5: הרצה

```bash
python expense_manager.py
```

### משתני סביבה (אופציונלי)

```bash
# נתיב מותאם לקובץ credentials
export EXPENSE_CREDENTIALS=/path/to/credentials.json

# שם הגיליון (כדי לדלג על השאלה בהפעלה)
export EXPENSE_SHEET_NAME="ניהול הוצאות"
```

## מבנה הגיליון

האפליקציה יוצרת אוטומטית שני גיליונות:

### גיליון "הוצאות"
| מזהה | תאריך | קטגוריה | תיאור | סכום | אמצעי תשלום | הערות | נוצר בתאריך |
|------|--------|----------|--------|------|-------------|-------|-------------|

### גיליון "תקציב"
| קטגוריה | תקציב חודשי | הערות |
|----------|-------------|-------|

## תכונות

- **הוספת הוצאות** - עם קטגוריה, תיאור, סכום ואמצעי תשלום
- **עריכה ומחיקה** - עדכון כל שדה או מחיקת הוצאות
- **חיפוש וסינון** - לפי קטגוריה, תאריכים, אמצעי תשלום, סכום או חיפוש חופשי
- **דשבורד חודשי** - ניתוח הוצאות חודשי עם גרפים, השוואה לתקציב ו-5 ההוצאות הגדולות
- **דשבורד שנתי** - מגמות שנתיות, פירוט חודשי והתפלגות לפי קטגוריה
- **ניהול תקציב** - הגדרת תקציב חודשי לכל קטגוריה ומעקב חריגות
- **ניתוח מגמות** - השוואה חודש-על-חודש, ממוצעים ותחזיות
